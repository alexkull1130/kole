{
  "name": "kole",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "description": "The standalone kole language interpreter",
  "scripts": {
    "test": "node --test",
    "start": "node src/cli.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
