# kole project instructions

- After each completed project update, run the relevant checks, create a descriptive Git commit, and push it to the configured GitHub remote.
- The user has authorized this ongoing commit-and-push workflow. Do not request confirmation again for ordinary project updates.
- Include only project-relevant changes. Inspect the working tree before staging and preserve unrelated user work.
- Never force-push or rewrite published history without explicit authorization. If verification or pushing fails, report the issue accurately and resolve it where possible.
