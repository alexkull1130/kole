class Constants {
    const version: string = "0.11";
    const tags: Set<string>();
    static main() -> void {
        const names: List<string>();
        names.add("Alex");
        const settings: Constants();
        settings.tags.add("kole");
        print(settings.version, names, settings.tags.toArray());
    }
}
