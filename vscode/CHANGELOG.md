# Changelog

## 0.11.0

- Added support for Kole 0.11 syntax, packages, generics, maps, sets, `const`, and switch expressions.
- Added ownership-specific diagnostic hints for `owns` and `belongsTo` relationships.
- Added typed completions, navigation, and diagnostics for unsaved files.
